export type ProjectType = {
    name: string
    image: string
    description: string
    techUsed: string[]
    liveDemo: string
    sourceCode: string
}

export type SkillType = {
    title:string,
    description:string,
    images:string[]
}